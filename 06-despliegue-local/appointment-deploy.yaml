apiVersion: apps/v1
kind: Deployment
metadata:
  name: appointment-deploy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: appointment-app
  template:
    metadata:
      labels:
        app: appointment-app
    spec:
      containers:
        - name: appointment
          image: localhost:5000/appointment
          imagePullPolicy: Always
      livenessProbe:
        httpGet:
          path: /healthz
          port: 8080
        initialDelaySeconds: 3
        periodSeconds: 3
